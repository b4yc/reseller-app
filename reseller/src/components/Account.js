import { IonPage } from "@ionic/react";
import React from "react";

const Account = () => {
  return (
    <React.Fragment>
      <IonPage>Account</IonPage>
    </React.Fragment>
  );
};

export default Account;
